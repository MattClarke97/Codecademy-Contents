var bucketList = ["Paraglide","Fly","Snowboard"];
var listItem = bucketList[2];

bucketList.push("item 3", "item 4");
//bucketList.pop()    This will delete the last item in an array.
console.log(listItem);
console.log(bucketList.length); //.length returns the number of items in the array
console.log(bucketList);
